import {
  Greens_default,
  Symbol,
  __commonJS,
  __toESM,
  arc_default,
  cross_default,
  drag_default,
  extent,
  hsl,
  max,
  pointer_default,
  rgb,
  select_default,
  sequential,
  triangle_default
} from "./chunk-ACGNN6GV.js";

// node_modules/bicubic-interpolate/index.js
var require_bicubic_interpolate = __commonJS({
  "node_modules/bicubic-interpolate/index.js"(exports, module) {
    function createInterpolator(values, options = {}) {
      options = Object.assign({
        extrapolate: false,
        scaleX: 1,
        scaleY: 1,
        translateX: 0,
        translateY: 0
      }, options);
      const a00 = values[1][1], a01 = -1 / 2 * values[1][0] + 1 / 2 * values[1][2], a02 = values[1][0] + -5 / 2 * values[1][1] + 2 * values[1][2] + -1 / 2 * values[1][3], a03 = -1 / 2 * values[1][0] + 3 / 2 * values[1][1] + -3 / 2 * values[1][2] + 1 / 2 * values[1][3], a10 = -1 / 2 * values[0][1] + 1 / 2 * values[2][1], a11 = 1 / 4 * values[0][0] + -1 / 4 * values[0][2] + -1 / 4 * values[2][0] + 1 / 4 * values[2][2], a12 = -1 / 2 * values[0][0] + 5 / 4 * values[0][1] + -1 * values[0][2] + 1 / 4 * values[0][3] + 1 / 2 * values[2][0] + -5 / 4 * values[2][1] + values[2][2] + -1 / 4 * values[2][3], a13 = 1 / 4 * values[0][0] + -3 / 4 * values[0][1] + 3 / 4 * values[0][2] + -1 / 4 * values[0][3] + -1 / 4 * values[2][0] + 3 / 4 * values[2][1] + -3 / 4 * values[2][2] + 1 / 4 * values[2][3], a20 = values[0][1] + -5 / 2 * values[1][1] + 2 * values[2][1] + -1 / 2 * values[3][1], a21 = -1 / 2 * values[0][0] + 1 / 2 * values[0][2] + 5 / 4 * values[1][0] + -5 / 4 * values[1][2] + -1 * values[2][0] + values[2][2] + 1 / 4 * values[3][0] + -1 / 4 * values[3][2], a22 = values[0][0] + -5 / 2 * values[0][1] + 2 * values[0][2] + -1 / 2 * values[0][3] + -5 / 2 * values[1][0] + 25 / 4 * values[1][1] + -5 * values[1][2] + 5 / 4 * values[1][3] + 2 * values[2][0] + -5 * values[2][1] + 4 * values[2][2] + -1 * values[2][3] + -1 / 2 * values[3][0] + 5 / 4 * values[3][1] + -1 * values[3][2] + 1 / 4 * values[3][3], a23 = -1 / 2 * values[0][0] + 3 / 2 * values[0][1] + -3 / 2 * values[0][2] + 1 / 2 * values[0][3] + 5 / 4 * values[1][0] + -15 / 4 * values[1][1] + 15 / 4 * values[1][2] + -5 / 4 * values[1][3] + -1 * values[2][0] + 3 * values[2][1] + -3 * values[2][2] + values[2][3] + 1 / 4 * values[3][0] + -3 / 4 * values[3][1] + 3 / 4 * values[3][2] + -1 / 4 * values[3][3], a30 = -1 / 2 * values[0][1] + 3 / 2 * values[1][1] + -3 / 2 * values[2][1] + 1 / 2 * values[3][1], a31 = 1 / 4 * values[0][0] + -1 / 4 * values[0][2] + -3 / 4 * values[1][0] + 3 / 4 * values[1][2] + 3 / 4 * values[2][0] + -3 / 4 * values[2][2] + -1 / 4 * values[3][0] + 1 / 4 * values[3][2], a32 = -1 / 2 * values[0][0] + 5 / 4 * values[0][1] + -1 * values[0][2] + 1 / 4 * values[0][3] + 3 / 2 * values[1][0] + -15 / 4 * values[1][1] + 3 * values[1][2] + -3 / 4 * values[1][3] + -3 / 2 * values[2][0] + 15 / 4 * values[2][1] + -3 * values[2][2] + 3 / 4 * values[2][3] + 1 / 2 * values[3][0] + -5 / 4 * values[3][1] + values[3][2] + -1 / 4 * values[3][3], a33 = 1 / 4 * values[0][0] + -3 / 4 * values[0][1] + 3 / 4 * values[0][2] + -1 / 4 * values[0][3] + -3 / 4 * values[1][0] + 9 / 4 * values[1][1] + -9 / 4 * values[1][2] + 3 / 4 * values[1][3] + 3 / 4 * values[2][0] + -9 / 4 * values[2][1] + 9 / 4 * values[2][2] + -3 / 4 * values[2][3] + -1 / 4 * values[3][0] + 3 / 4 * values[3][1] + -3 / 4 * values[3][2] + 1 / 4 * values[3][3];
      return (x, y) => {
        x = x * options.scaleX + options.translateX;
        y = y * options.scaleY + options.translateY;
        if (x < 0 || y < 0 || x > 1 || y > 1) throw "cannot interpolate outside the square from (0, 0) to (1, 1): (" + x + ", " + y + ")";
        const x2 = x * x, x3 = x * x2, y2 = y * y, y3 = y * y2;
        return a00 + a01 * y + a02 * y2 + a03 * y3 + (a10 + a11 * y + a12 * y2 + a13 * y3) * x + (a20 + a21 * y + a22 * y2 + a23 * y3) * x2 + (a30 + a31 * y + a32 * y2 + a33 * y3) * x3;
      };
    }
    function createGridInterpolator2(values, options = {}) {
      options = Object.assign({
        extrapolate: false,
        scaleX: 1,
        scaleY: 1,
        translateX: 0,
        translateY: 0
      }, options);
      const m = values.length;
      const n = values[0].length;
      const interpolators = [];
      if (options.extrapolate) {
        values[-2] = [];
        values[-1] = [];
        values[m] = [];
        values[m + 1] = [];
        for (var y = 0; y < n; y++) {
          const leftDelta = values[0][y] - values[1][y];
          const rightDelta = values[m - 1][y] - values[m - 2][y];
          values[-2][y] = values[0][y] + 2 * leftDelta;
          values[-1][y] = values[0][y] + leftDelta;
          values[m][y] = values[m - 1][y] + rightDelta;
          values[m + 1][y] = values[m - 1][y] + 2 * rightDelta;
        }
        for (var x = -2; x < m + 2; x++) {
          const bottomDelta = values[x][0] - values[x][1];
          const topDelta = values[x][n - 1] - values[x][n - 2];
          values[x][-2] = values[x][0] + 2 * bottomDelta;
          values[x][-1] = values[x][0] + bottomDelta;
          values[x][n] = values[x][n - 1] + topDelta;
          values[x][n + 1] = values[x][n - 1] + 2 * topDelta;
        }
        for (var x = -1; x < m; x++) interpolators[x] = [];
      } else {
        for (var x = 1; x < m - 2; x++) interpolators[x] = [];
      }
      return (x2, y2) => {
        x2 = x2 * options.scaleX + options.translateX;
        y2 = y2 * options.scaleY + options.translateY;
        if (options.extrapolate) {
          if (x2 < -1 || y2 < -1 || x2 > m || y2 > n) throw "cannot interpolate outside the rectangle from (-1, -1) to (" + m + ", " + n + ") even when extrapolating: (" + x2 + ", " + y2 + ")";
        } else {
          if (x2 < 1 || y2 < 1 || x2 > m - 2 || y2 > n - 2) throw "cannot interpolate outside the rectangle from (1, 1) to (" + (m - 2) + ", " + (n - 2) + "): (" + x2 + ", " + y2 + "), you might want to enable extrapolating";
        }
        var blX = Math.floor(x2);
        var blY = Math.floor(y2);
        if (options.extrapolate) {
          if (x2 === m) blX--;
          if (y2 === n) blY--;
        } else {
          if (x2 === m - 2) blX--;
          if (y2 === n - 2) blY--;
        }
        if (!interpolators[blX][blY]) {
          interpolators[blX][blY] = createInterpolator([
            [values[blX - 1][blY - 1], values[blX - 1][blY], values[blX - 1][blY + 1], values[blX - 1][blY + 2]],
            [values[blX + 0][blY - 1], values[blX + 0][blY], values[blX][blY + 1], values[blX][blY + 2]],
            [values[blX + 1][blY - 1], values[blX + 1][blY], values[blX + 1][blY + 1], values[blX + 1][blY + 2]],
            [values[blX + 2][blY - 1], values[blX + 2][blY], values[blX + 2][blY + 1], values[blX + 2][blY + 2]]
          ], {
            translateX: -blX,
            translateY: -blY
          });
        }
        const interpolator = interpolators[blX][blY];
        return interpolator(x2, y2);
      };
    }
    function createMultiInterpolator(values, options = {}) {
      const s = values[0][0].length;
      const interpolators = [];
      for (var i = 0; i < s; i++) interpolators[i] = createInterpolator(values.map((col) => col.map((vals) => vals[i])), options);
      return (x, y) => {
        return interpolators.map((interpolator) => interpolator(x, y));
      };
    }
    function createMultiGridInterpolator(values, options = {}) {
      const s = values[0][0].length;
      const interpolators = [];
      for (var i = 0; i < s; i++) interpolators[i] = createGridInterpolator2(values.map((col) => col.map((vals) => vals[i])), options);
      return (x, y) => {
        return interpolators.map((interpolator) => interpolator(x, y));
      };
    }
    module.exports = {
      createInterpolator,
      createGridInterpolator: createGridInterpolator2,
      createMultiInterpolator,
      createMultiGridInterpolator
    };
  }
});

// node_modules/d3-soccer/src/config.js
var pitchLenght = 105;
var pitchWidth = 68;
var spadlResults = [
  { label: "Fail" },
  { label: "Success" },
  { label: "Offside" },
  { label: "Own goal" },
  { label: "Yellow card" },
  { label: "Red card" }
];
var spadlActionTypes = [
  { label: "Pass" },
  { label: "Cross" },
  { label: "Throw in" },
  { label: "Freekick (cross)" },
  { label: "Freekick (short)" },
  { label: "Corner (cross)" },
  { label: "Corner (short)" },
  { label: "Dribble" },
  { label: "Foul" },
  { label: "Tackle" },
  { label: "Interception" },
  { label: "Shot" },
  { label: "Penalty" },
  { label: "Freekick (shot)" },
  { label: "Save" },
  { label: "Claim" },
  { label: "Punch" },
  { label: "Pick up" },
  { label: "Clearance" },
  { label: "Bad touch" },
  { label: "-" },
  { label: "Carry" },
  { label: "Goal kick" }
];
var spadlBodyparts = [
  { label: "Foot" },
  { label: "Head" },
  { label: "Other" }
];

// node_modules/d3-soccer/src/charts/pitch.js
function pitch_default() {
  var clip = { top: 0, right: pitchLenght, bottom: pitchWidth, left: 0 };
  var height = 300;
  var rotated = false;
  var width = (-clip.left + clip.right) / (-clip.top + clip.bottom) * height;
  var pitchstrokewidth = 0.5;
  var dirOfPlay = false;
  var shadeMiddleThird = false;
  var drawGoalsFn = drawGoalsAsLine;
  function drawGoalsAsBox(lines) {
    lines.append("rect").attr("stroke-width", pitchstrokewidth).attr("fill", "none").attr("x", -2).attr("y", pitchWidth / 2 - 3.66).attr("width", 2).attr("height", 7.32);
    lines.append("rect").attr("stroke-width", pitchstrokewidth).attr("fill", "none").attr("x", pitchLenght).attr("y", pitchWidth / 2 - 3.66).attr("width", 2).attr("height", 7.32);
  }
  function drawGoalsAsLine(lines) {
    lines.append("rect").attr("stroke-width", 0).attr("x", -pitchstrokewidth * 1.5).attr("y", pitchWidth / 2 - 3.66).attr("width", pitchstrokewidth * 3).attr("height", 7.32);
    lines.append("rect").attr("stroke-width", 0).attr("x", pitchLenght - pitchstrokewidth * 1.5).attr("y", pitchWidth / 2 - 3.66).attr("width", pitchstrokewidth * 3).attr("height", 7.32);
  }
  function chart(selection) {
    selection.each(function() {
      var pitch = select_default(this).append("svg").attr("width", width).attr("height", height).attr("viewBox", () => {
        let width2 = clip.right - clip.left;
        let height2 = clip.bottom - clip.top;
        let xdim, ydim, xpad, ypad;
        if (rotated) {
          xpad = height2 === pitchWidth ? 4 : 2;
          ypad = width2 === pitchLenght ? 4 : 2;
          xdim = -clip.left + clip.right + ypad;
          ydim = -clip.top + clip.bottom + xpad;
        } else {
          xpad = height2 === pitchWidth ? 4 : 2;
          ypad = width2 === pitchLenght ? 4 : 2;
          ydim = -clip.top + clip.bottom + xpad;
          xdim = -clip.left + clip.right + ypad;
        }
        return `-2 -2 ${xdim} ${ydim}`;
      }).append("g").attr("class", "pitch").attr(
        "transform",
        `translate(${-clip.left}, ${-clip.top})rotate(${rotated ? -90 : 0} 0 0)translate(${rotated ? -105 : 0} 0)`
      );
      pitch.append("g").attr("class", "below");
      var lines = pitch.append("g").attr("class", "lines").attr("stroke", "#000").attr("fill", "#000").attr("pointer-events", "none");
      lines.append("line").attr("stroke-width", pitchstrokewidth).attr("x1", pitchLenght / 2).attr("y1", 0).attr("x2", pitchLenght / 2).attr("y2", pitchWidth);
      lines.append("circle").attr("stroke-width", pitchstrokewidth).attr("fill", "none").attr("cx", pitchLenght / 2).attr("cy", pitchWidth / 2).attr("r", 9.15);
      lines.append("circle").attr("stroke-width", 0).attr("cx", pitchLenght / 2).attr("cy", pitchWidth / 2).attr("r", pitchstrokewidth);
      var arc1 = arc_default().innerRadius(9.15).outerRadius(9.15).startAngle(38 * (Math.PI / 180)).endAngle(142 * (Math.PI / 180));
      lines.append("path").attr("stroke-width", pitchstrokewidth).attr("d", arc1).attr("transform", "translate(11," + pitchWidth / 2 + ")");
      var arc2 = arc_default().innerRadius(9.15).outerRadius(9.15).startAngle(218 * (Math.PI / 180)).endAngle(322 * (Math.PI / 180));
      lines.append("path").attr("stroke-width", pitchstrokewidth).attr("d", arc2).attr(
        "transform",
        "translate(" + (pitchLenght - 11) + "," + pitchWidth / 2 + ")"
      );
      lines.append("rect").attr("stroke-width", pitchstrokewidth).attr("fill", "none").attr("x", 0).attr("y", pitchWidth / 2 - 9.16).attr("width", 5.5).attr("height", 18.32);
      lines.append("rect").attr("stroke-width", pitchstrokewidth).attr("fill", "none").attr("x", pitchLenght - 5.5).attr("y", pitchWidth / 2 - 9.16).attr("width", 5.5).attr("height", 18.32);
      lines.append("rect").attr("stroke-width", pitchstrokewidth).attr("fill", "none").attr("x", 0).attr("y", pitchWidth / 2 - 20.16).attr("width", 16.5).attr("height", 40.32);
      lines.append("rect").attr("stroke-width", pitchstrokewidth).attr("fill", "none").attr("x", pitchLenght - 16.5).attr("y", pitchWidth / 2 - 20.16).attr("width", 16.5).attr("height", 40.32);
      lines.append("circle").attr("stroke-width", 0).attr("cx", 11).attr("cy", pitchWidth / 2).attr("r", pitchstrokewidth);
      lines.append("circle").attr("stroke-width", 0).attr("cx", pitchLenght - 11).attr("cy", pitchWidth / 2).attr("r", pitchstrokewidth);
      if (dirOfPlay) {
        lines.append("polygon").attr("opacity", 0.1).attr("stroke-width", 0).attr("class", "shaded").attr(
          "points",
          `
                  25,${pitchWidth / 2 - 2} 
                  35,${pitchWidth / 2 - 2} 
                  35,${pitchWidth / 2 - 5} 
                  40,${pitchWidth / 2} 
                  35,${pitchWidth / 2 + 5} 
                  35,${pitchWidth / 2 + 2} 
                  25,${pitchWidth / 2 + 2} 
                  25,${pitchWidth / 2 - 2}
                `
        );
      }
      lines.append("rect").attr("stroke-width", pitchstrokewidth).attr("fill", "none").attr("x", 0).attr("y", 0).attr("width", pitchLenght).attr("height", pitchWidth);
      drawGoalsFn(lines);
      pitch.append("g").attr("class", "above");
      if (shadeMiddleThird) {
        lines.append("rect").attr("opacity", 0.1).attr("class", "shaded").attr("x", 35).attr("y", 0).attr("width", 35).attr("height", pitchWidth);
      }
    });
  }
  chart.height = function(_) {
    if (!arguments.length) return height;
    height = +_;
    width = (-clip.left + clip.right) / (-clip.top + clip.bottom) * height;
    return chart;
  };
  chart.width = function() {
    return width;
  };
  chart.rotate = function(_) {
    if (!arguments.length) return rotated;
    rotated = Boolean(_);
    return chart;
  };
  chart.showDirOfPlay = function(_) {
    if (!arguments.length) return dirOfPlay;
    dirOfPlay = Boolean(_);
    return chart;
  };
  chart.shadeMiddleThird = function(_) {
    if (!arguments.length) return shadeMiddleThird;
    shadeMiddleThird = Boolean(_);
    return chart;
  };
  chart.pitchStrokeWidth = function(_) {
    if (!arguments.length) return pitchstrokewidth;
    pitchstrokewidth = +_;
    return chart;
  };
  chart.goals = function(_) {
    if (!arguments.length) return drawGoalsFn;
    if (_ === "box") drawGoalsFn = drawGoalsAsBox;
    else if (_ === "line") drawGoalsFn = drawGoalsAsLine;
    else drawGoalsFn = _;
    return chart;
  };
  chart.clip = function(_) {
    if (!arguments.length)
      return [
        [clip.left, clip.top],
        [clip.right, clip.bottom]
      ];
    clip = { top: _[0][1], bottom: _[1][1], left: _[0][0], right: _[1][0] };
    width = (-clip.left + clip.right) / (-clip.top + clip.bottom) * height;
    return chart;
  };
  return chart;
}

// node_modules/d3-soccer/src/charts/heatmap.js
var import_bicubic_interpolate = __toESM(require_bicubic_interpolate(), 1);
function position(el, parent) {
  var elPos = el.node().getBoundingClientRect();
  var vpPos = parent.node().getBoundingClientRect();
  return {
    top: elPos.top - vpPos.top,
    left: elPos.left - vpPos.left,
    width: elPos.width,
    bottom: elPos.bottom - vpPos.top,
    height: elPos.height,
    right: elPos.right - vpPos.left
  };
}
var grid = function() {
  function grid2(d) {
    var data = [];
    var gx = d.length;
    var gy = d[0].length;
    var incx = pitchLenght / gx;
    var incy = pitchWidth / gy;
    var max_v = 0;
    for (var x = 0; x < gx; x++) {
      for (var y = 0; y < gy; y++) {
        if (d[x][y] > max_v) {
          max_v = d[x][y];
        }
        data.push({
          i: x,
          j: y,
          x: x * incx,
          y: y * incy,
          width: incx,
          height: incy,
          value: d[x][y]
        });
      }
    }
    return data;
  }
  return grid2;
};
var rectbin = function() {
  var dx = 0.1, dy = 0.1, x = (d) => d[0], y = (d) => d[1];
  function trunc(x2) {
    return x2 < 0 ? Math.ceil(x2) : Math.floor(x2);
  }
  function rectbin2(points) {
    var binsById = {};
    var xExtent = [0, 105];
    var yExtent = [0, 68];
    for (var Y = yExtent[0], pj = 0; Y < yExtent[1] - 1e-4; Y += dy, pj++) {
      for (var X = xExtent[0], pi = 0; X < xExtent[1] - 1e-4; X += dx, pi++) {
        var id = pi + "-" + pj;
        var bin = binsById[id] = [];
        bin.i = pi;
        bin.j = pj;
        bin.x = X;
        bin.y = Y;
        bin.width = dx;
        bin.height = dy;
        bin.value = 0;
      }
    }
    points.forEach(function(point, i) {
      var py = y.call(rectbin2, point, i) / dy;
      var pj2 = trunc(py);
      var px = x.call(rectbin2, point, i) / dx;
      var pi2 = trunc(px);
      var id2 = pi2 + "-" + pj2;
      var bin2 = binsById[id2];
      if (bin2) {
        bin2.push(point);
        bin2.value += 1;
      }
    });
    return Object.values(binsById);
  }
  rectbin2.x = function(_) {
    if (!arguments.length) return x;
    x = _;
    return rectbin2;
  };
  rectbin2.y = function(_) {
    if (!arguments.length) return y;
    y = _;
    return rectbin2;
  };
  rectbin2.dx = function(_) {
    if (!arguments.length) return dx;
    dx = _;
    return rectbin2;
  };
  rectbin2.dy = function(_) {
    if (!arguments.length) return dy;
    dy = _;
    return rectbin2;
  };
  return rectbin2;
};
function heatmap_default(pitch) {
  var enableInteraction = false, selected = [void 0, void 0], onSelect = function() {
    return;
  }, onDeselect = function() {
    return;
  }, color = sequential(Greens_default).domain([
    void 0,
    void 0
  ]), stroke = "#00000011", selStroke = "#FF6600", strokewidth = 0, interpolated = false, showValues = false, valueFormatter = (v) => v, valueFontSize = (h) => h * 0.2 + "px", parent_el = "body", selStrokewidth = 0.5;
  function chart(g) {
    g.each(function(data) {
      var selx = selected[0];
      var sely = selected[1];
      if (color.domain().some(isNaN)) {
        color.domain(extent(data, (d) => d.value));
      }
      var draw = select_default(this).call(pitch).select(".below");
      var join = draw.selectAll("rect.cell").data(data);
      var enterSel = join.enter().append("g").attr("class", "cell").attr("id", (d) => `cell(${d.i},${d.j})`);
      enterSel.append("rect").attr("x", (d) => d.x).attr("y", (d) => d.y).attr("width", (d) => d.width).attr("height", (d) => d.height).attr("data", (d) => d.value).style("stroke", function(d) {
        return selx === d.x && sely === d.y ? selStroke : stroke;
      }).style("stroke-width", function(d) {
        return selx === d.x && sely === d.y ? selStrokewidth : strokewidth;
      }).style("fill", function(d) {
        return interpolated ? "transparent" : color(+d.value);
      }).style("cursor", enableInteraction ? "crosshair" : "default").on("mouseover", function(_, d) {
        if (enableInteraction)
          select_default(this).style("stroke", selStroke).style("stroke-width", selStrokewidth);
        onSelect(d.x, d.y, d.value);
      }).on("mouseout", function(_, d) {
        if (enableInteraction)
          select_default(this).style("stroke", stroke).style("stroke-width", strokewidth);
        onDeselect(d.x, d.y, d.value);
      });
      join.merge(enterSel).select("rect").attr("x", (d) => d.x).attr("y", (d) => d.y).attr("width", (d) => d.width).attr("height", (d) => d.height).attr("data", (d) => d.value).style("fill", function(d) {
        return interpolated ? "transparent" : color(+d.value);
      });
      join.exit().select("rect").attr("width", 0).attr("height", 0).remove();
      if (showValues) {
        enterSel.append("text").attr("x", (d) => d.x + d.width / 2).attr("y", (d) => d.y + d.height / 2).attr("dy", ".35em").attr("text-anchor", "middle").text((d) => valueFormatter(d.value)).style("fill", "white").style("mix-blend-mode", "difference").style("font-size", (d) => valueFontSize(d.height, d.width, d.value)).style("pointer-events", "none");
        join.merge(enterSel).select("text").attr("x", (d) => d.x + d.width / 2).attr("y", (d) => d.y + d.height / 2).text((d) => valueFormatter(d.value));
        join.exit().select("text").style("font-size", 0).remove();
      }
      if (interpolated) {
        var gx = max(data, (d) => d.i) + 1;
        var gy = max(data, (d) => d.j) + 1;
        var grid2 = [];
        for (var x = 0; x < gx; x++) {
          grid2.push([]);
          for (var y = 0; y < gy; y++) {
            grid2[x].push(data.find((d) => d.i === x && d.j === y).value);
          }
        }
        var canvas = select_default(parent_el).style("position", "relative").append("canvas").style("z-index", -1).style("position", "absolute").style("pointer-events", "none");
        var bbox = position(draw, select_default(parent_el));
        var n = parseInt(bbox.height);
        var m = parseInt(bbox.width);
        var scaleX = (pitch.clip()[1][0] - pitch.clip()[0][0]) / 105;
        var scaleY = (pitch.clip()[1][1] - pitch.clip()[0][1]) / 68;
        if (pitch.rotate()) {
          scaleY = (pitch.clip()[1][1] - pitch.clip()[0][1]) / 105;
          scaleX = (pitch.clip()[1][0] - pitch.clip()[0][0]) / 68;
        }
        canvas.attr("width", m * scaleX).attr("height", n * scaleY).style("left", bbox.left + "px").style("top", bbox.top + "px");
        const gridInterpolator = (0, import_bicubic_interpolate.createGridInterpolator)(grid2, {
          extrapolate: true,
          scaleX: pitch.rotate() ? gx / n : gx / m,
          scaleY: pitch.rotate() ? gy / m : gy / n,
          translateX: -0.5,
          translateY: -0.5
        });
        var context = canvas.node().getContext("2d"), image = context.createImageData(m, n);
        var l = 0;
        for (var j = 0; j < n; ++j) {
          for (var i = 0; i < m; ++i) {
            var v;
            if (pitch.rotate()) v = gridInterpolator(n - j, i);
            else v = gridInterpolator(i, j);
            var c = rgb(color(v || 0));
            image.data[l + 0] = c.r;
            image.data[l + 1] = c.g;
            image.data[l + 2] = c.b;
            image.data[l + 3] = 255;
            l += 4;
          }
        }
        context.putImageData(image, 0, 0);
      }
    });
  }
  chart.colorScale = function(_) {
    if (!arguments.length) return color;
    color = _;
    return chart;
  };
  chart.selected = function(_) {
    if (!arguments.length) return selected;
    selected = _;
    return chart;
  };
  chart.enableInteraction = function(_) {
    if (!arguments.length) return enableInteraction;
    enableInteraction = Boolean(_);
    return chart;
  };
  chart.showValues = function(_) {
    if (!arguments.length) return showValues;
    showValues = Boolean(_);
    return chart;
  };
  chart.valueFormatter = function(_) {
    if (!arguments.length) return valueFormatter;
    valueFormatter = _;
    return chart;
  };
  chart.valueFontSize = function(_) {
    if (!arguments.length) return valueFontSize;
    valueFontSize = _;
    return chart;
  };
  chart.interpolate = function(_) {
    if (!arguments.length) return interpolated;
    interpolated = Boolean(_);
    return chart;
  };
  chart.parent_el = function(_) {
    if (!arguments.length) return parent_el;
    parent_el = _;
    return chart;
  };
  chart.onSelect = function(f) {
    onSelect = f;
    return chart;
  };
  chart.onDeselect = function(f) {
    onDeselect = f;
    return chart;
  };
  return chart;
}

// node_modules/d3-soccer/src/charts/actions.js
function actions_default(pitch) {
  var scale = 4;
  var showTooltip = false;
  var draggable = false;
  var teamColors = {};
  var onUpdate = () => void 0;
  function chart(g) {
    g.each(function(data) {
      var actionsLayer = select_default(this).call(pitch).select(".above");
      actionsLayer.append("svg:defs").append("svg:marker").attr("id", "triangle").attr("refX", 11).attr("refY", 6).attr("dy", 6).attr("markerWidth", 12).attr("markerHeight", 12).attr("orient", "auto").append("path").attr("d", "M 0 0 12 6 0 12 3 6");
      var dragHandler = drag_default().on("start", function() {
        select_default(this).classed("active", true);
      }).on("drag", function(d) {
        var [x, y] = pointer_default(actionsLayer.node());
        var marker = select_default(this).attr("marker");
        select_default(this).attr("transform", "translate(" + x + "," + y + ")");
        var action = select_default(`#action-${d.action_id}`);
        if (marker === "marker-start") {
          action.select("line").attr("x1", x).attr("y1", y);
          var prev_action = select_default(`#action-${d.action_id - 1}`);
          prev_action.select("line").attr("x2", x).attr("y2", y);
        } else {
          action.select("line").attr("x2", x).attr("y2", y);
        }
      }).on("end", function(d) {
        var [x, y] = pointer_default(actionsLayer.node());
        var marker = select_default(this).attr("marker");
        select_default(this).classed("active", false);
        var newData = data.slice();
        if (marker === "marker-start") {
          newData.find((a) => a.action_id === d.action_id)["start_x"] = x;
          newData.find((a) => a.action_id === d.action_id)["start_y"] = 68 - y;
          var prevAction = newData.find(
            (a) => a.action_id === d.action_id - 1
          );
          if (prevAction) {
            prevAction["end_x"] = x;
            prevAction["end_y"] = 68 - y;
          }
        } else {
          newData.find((a) => a.action_id === d.action_id)["end_x"] = x;
          newData.find((a) => a.action_id === d.action_id)["end_y"] = 68 - y;
        }
        onUpdate(newData);
      });
      var defaultColors = ["#FDB913", "#87CEEB"];
      for (const action of data) {
        if (action.team_id in teamColors === false) {
          if (Object.keys(teamColors).length >= 2)
            throw "You specified ids of teams that do not occur in the data!";
          else
            teamColors[action.team_id] = defaultColors[Object.keys(teamColors).length];
        }
      }
      var update = actionsLayer.selectAll("g.action").data(data);
      update.each(function(d, i) {
        select_default(this).selectAll("*").remove();
        var action = select_default(this).attr("class", "action").attr("id", `action-${d.action_id}`).call(
          actiontypePlotFn[d.type_id]["plot"],
          d,
          i + 1,
          teamColors[d.team_id],
          scale
        );
        action.selectAll("g.symbol").on("mouseover", (e, d2) => showTooltip && showTooltip.show(e, d2)).on("mouseout", () => showTooltip && showTooltip.hide());
      });
      update.enter().append("g").attr("class", "action").each(function(d, i) {
        var action = select_default(this).attr("class", "action").attr("id", `action-${d.action_id}`).call(
          actiontypePlotFn[d.type_id]["plot"],
          d,
          i + 1,
          teamColors[d.team_id],
          scale
        );
        action.selectAll("g.symbol").on("mouseover", (e, d2) => showTooltip && showTooltip.show(e, d2)).on("mouseout", () => showTooltip && showTooltip.hide());
      });
      var last_action = actionsLayer.selectAll(".action:last-of-type");
      var mend = last_action.selectAll("g.marker-end").data((d) => [d]).enter().append("g").attr("class", "marker marker-end").attr("marker", "marker-end").attr("transform", function(d) {
        return "translate(" + d.end_x + "," + (68 - d.end_y) + ")";
      });
      mend.append("circle").attr("stroke-width", 0).attr("fill", "#000").attr("opacity", 0).attr("r", 2);
      update.exit().remove();
      if (draggable) dragHandler(actionsLayer.selectAll("g.marker"));
    });
  }
  chart.showTooltip = function(_) {
    if (!arguments.length) return showTooltip;
    showTooltip = _;
    return chart;
  };
  chart.scale = function(_) {
    if (!arguments.length) return scale;
    scale = _;
    return chart;
  };
  chart.draggable = function(_) {
    if (!arguments.length) return draggable;
    draggable = Boolean(_);
    return chart;
  };
  chart.teamColors = function(_) {
    if (!arguments.length) return teamColors;
    teamColors = _;
    return chart;
  };
  chart.onUpdate = function(f) {
    if (!arguments.length) return onUpdate;
    onUpdate = f;
    return chart;
  };
  return chart;
}
var actiontypePlotFn = [
  { label: "Pass", plot: plotPass, symbol: symbolPass },
  { label: "Cross", plot: plotPass, symbol: symbolPass },
  { label: "Throw in", plot: plotPass, symbol: symbolPass },
  { label: "Freekick (cross)", plot: plotPass, symbol: symbolPass },
  { label: "Freekick (short)", plot: plotPass, symbol: symbolPass },
  { label: "Corner (cross)", plot: plotPass, symbol: symbolPass },
  { label: "Corner (short)", plot: plotPass, symbol: symbolPass },
  { label: "Dribble", plot: plotDribble, symbol: symbolDribble },
  { label: "Foul", plot: plotFoulWon, symbol: symbolFoulWon },
  { label: "Tackle", plot: plotBlock, symbol: symbolBlock },
  { label: "Interception", plot: plotBlock, symbol: symbolBlock },
  { label: "Shot", plot: plotShot, symbol: symbolShot },
  { label: "Penalty", plot: plotShot, symbol: symbolShot },
  { label: "Freekick (shot)", plot: plotShot, symbol: symbolShot },
  { label: "Save", plot: plotBlock, symbol: symbolBlock },
  { label: "Claim", plot: plotBlock, symbol: symbolBlock },
  { label: "Punch", plot: plotClearance, symbol: symbolClearance },
  { label: "Pick up", plot: plotBlock, symbol: symbolBlock },
  { label: "Clearance", plot: plotClearance, symbol: symbolClearance },
  { label: "Bad touch", plot: plotBlock, symbol: symbolBlock },
  { label: "-", plot: () => null, symbol: () => null },
  { label: "Carry", plot: plotCarry, symbol: () => null },
  { label: "Goal kick", plot: plotPass, symbol: symbolPass }
];
function plotShot(g, d, i, color, scale) {
  let strokeWidth = 0.1 * scale;
  let shot = g.classed("shot", true);
  shot.append("line").attr("x1", function(d2) {
    return d2.start_x;
  }).attr("y1", function(d2) {
    return 68 - d2.start_y;
  }).attr("x2", function(d2) {
    return d2.end_x;
  }).attr("y2", function(d2) {
    return 68 - d2.end_y;
  }).attr("stroke-width", strokeWidth).attr("stroke", "black").attr("marker-end", "url(#triangle)");
  var mstart = shot.append("g").attr("class", "marker").attr("marker", "marker-start").attr("transform", function(d2) {
    return "translate(" + d2.start_x + "," + (68 - d2.start_y) + ")";
  });
  mstart.call(symbolShot, d, i, color, scale);
}
function symbolShot(g, d, i, color, scale) {
  let radius = 1 * scale;
  let strokeWidth = 0.1 * scale;
  let shot = g.append("g").attr("class", "symbol shot");
  shot.append("circle").attr("r", radius).attr("stroke-width", strokeWidth).attr("stroke", "grey").attr("fill", color);
  shot.append("text").attr("text-anchor", "middle").attr("dominant-baseline", "central").style("font-size", radius * 0.8 + "px").attr("fill", hsl(color).l >= 0.6 ? "#000" : "#fff").text(i);
}
function plotPass(g, d, i, color, scale) {
  let strokeWidth = 0.1 * scale;
  let pass = g.classed("pass", true);
  pass.append("line").attr("x1", function(d2) {
    return d2.start_x;
  }).attr("y1", function(d2) {
    return 68 - d2.start_y;
  }).attr("x2", function(d2) {
    return d2.end_x;
  }).attr("y2", function(d2) {
    return 68 - d2.end_y;
  }).attr("stroke-width", strokeWidth).attr("stroke", "#000").attr("marker-end", "url(#triangle)");
  var mstart = pass.append("g").attr("class", "marker").attr("marker", "marker-start").attr("transform", function(d2) {
    return "translate(" + d2.start_x + "," + (68 - d2.start_y) + ")";
  });
  mstart.call(symbolPass, d, i, color, scale);
}
function symbolPass(g, d, i, color, scale) {
  let radius = 1 * scale;
  let strokeWidth = 0.1 * scale;
  let pass = g.append("g").attr("class", "symbol pass");
  pass.append("circle").attr("r", radius).attr("stroke-width", strokeWidth).attr("stroke", color).attr("fill", "#fff");
  pass.append("text").attr("text-anchor", "middle").attr("dominant-baseline", "central").style("font-size", radius * 0.8 + "px").attr("fill", "#000").text(i);
}
function plotDribble(g, d, i, color, scale) {
  let strokeWidth = 0.1 * scale;
  let dribble = g.classed("dribble", true);
  dribble.append("line").attr("x1", function(d2) {
    return d2.start_x;
  }).attr("y1", function(d2) {
    return 68 - d2.start_y;
  }).attr("x2", function(d2) {
    return d2.end_x;
  }).attr("y2", function(d2) {
    return 68 - d2.end_y;
  }).attr("stroke-width", strokeWidth).style("stroke-dasharray", "1,1").attr("stroke", "black").attr("marker-end", "url(#triangle)");
  var mstart = dribble.append("g").attr("class", "marker").attr("marker", "marker-start").attr("transform", function(d2) {
    return "translate(" + d2.start_x + "," + (68 - d2.start_y) + ")";
  });
  mstart.call(symbolDribble, d, i, color, scale);
}
function symbolDribble(g, d, i, color, scale) {
  let radius = 1 * scale;
  let strokeWidth = 0.1 * scale;
  let dribble = g.append("g").attr("class", "symbol dribble");
  dribble.append("circle").attr("r", radius).attr("stroke-width", strokeWidth).attr("stroke", color).attr("fill", "#fff");
  dribble.append("text").attr("text-anchor", "middle").attr("dominant-baseline", "central").style("font-size", radius * 0.8 + "px").attr("fill", "#000").text(i);
}
function plotCarry(g, d, i, color, scale) {
  let strokeWidth = 0.05;
  let radius = 1 * scale;
  let carry = g.classed("carry", true);
  carry.append("line").attr("x1", function(d2) {
    return d2.start_x;
  }).attr("y1", function(d2) {
    return 68 - d2.start_y;
  }).attr("x2", function(d2) {
    return d2.end_x;
  }).attr("y2", function(d2) {
    return 68 - d2.end_y;
  }).attr("stroke-width", strokeWidth).style("stroke-dasharray", "0.4,0.4").attr("stroke", "grey");
  var mstart = carry.append("g").attr("class", "marker").attr("marker", "marker-start").attr("transform", function(d2) {
    return "translate(" + d2.start_x + "," + (68 - d2.start_y) + ")";
  });
  mstart.append("circle").attr("stroke-width", 0).attr("fill", "#000").attr("opacity", 0).attr("r", radius);
}
function plotClearance(g, d, i, color, scale) {
  let clearance = g.classed("clearance", true);
  var mstart = clearance.append("g").attr("class", "marker").attr("marker", "marker-start").attr("transform", function(d2) {
    return "translate(" + d2.start_x + "," + (68 - d2.start_y) + ")";
  });
  mstart.call(symbolClearance, d, i, color, scale);
}
function symbolClearance(g, d, i, color, scale) {
  var size = 2 * scale * scale;
  let strokeWidth = 0.1 * scale;
  let clearance = g.append("g").attr("class", "symbol clearance");
  var triangle = Symbol().size(size).type(triangle_default);
  clearance.append("path").attr("fill", color).attr("stroke", "grey").attr("stroke-width", strokeWidth).attr("d", triangle);
  clearance.append("text").attr("dominant-baseline", "central").style("text-anchor", "middle").style("font-size", scale * 0.8 + "px").attr("fill", hsl(color).l >= 0.6 ? "#000" : "#fff").text(i);
}
function plotBlock(g, d, i, color, scale) {
  let block = g.classed("block", true);
  var mstart = block.append("g").attr("class", "marker").attr("marker", "marker-start").attr("transform", function(d2) {
    return "translate(" + d2.start_x + "," + (68 - d2.start_y) + ")";
  });
  mstart.call(symbolBlock, d, i, color, scale);
}
function symbolBlock(g, d, i, color, scale) {
  var size = 2 * scale;
  let strokeWidth = 0.1 * scale;
  let block = g.append("g").attr("class", "symbol block");
  block.append("rect").attr("x", -size / 2).attr("y", -size / 2).attr("width", size).attr("height", size).attr("stroke-width", strokeWidth).attr("stroke", "grey").attr("fill", color);
  block.append("text").attr("text-anchor", "middle").attr("dominant-baseline", "central").style("text-anchor", "middle").style("font-size", size * 0.4 + "px").attr("fill", hsl(color).l >= 0.6 ? "#000" : "#fff").text(i);
}
function plotFoulWon(g, d, i, color, scale) {
  let foul = g.classed("foul", true);
  var mstart = foul.append("g").attr("class", "marker").attr("marker", "marker-start").attr("transform", function(d2) {
    return "translate(" + d2.start_x + "," + (68 - d2.start_y) + ")";
  });
  mstart.call(symbolFoulWon, d, i, color, scale);
}
function symbolFoulWon(g, d, i, color, scale) {
  var size = 2 * scale * scale;
  let strokeWidth = 0.1 * scale;
  let foul = g.append("g").attr("class", "symbol foul");
  var symbol = Symbol().size(size).type(cross_default);
  foul.append("path").attr("fill", color).attr("stroke", "grey").attr("stroke-width", strokeWidth).attr("d", symbol);
  foul.append("text").attr("dominant-baseline", "central").style("text-anchor", "middle").style("font-size", scale * 0.8 + "px").attr("fill", hsl(color).l >= 0.6 ? "#000" : "#fff").text(i);
}

// node_modules/d3-soccer/src/charts/actions_table.js
function actions_table_default() {
  var scale = 9, tableColumns = {
    symbol: "",
    team_name: "Team",
    player_name: "Player",
    type_id: "Type",
    result_id: "Result"
  }, teamColors = {};
  function tabulate(g, columns) {
    g.html("");
    var table = g.append("table"), thead = table.append("thead"), tbody = table.append("tbody");
    table.attr("class", "actions-table");
    thead.append("tr").selectAll("th").data(Object.values(columns)).enter().append("th").text(function(column) {
      return column;
    });
    return tbody;
  }
  function chart(g) {
    g.each(function(data) {
      var defaultColors = ["#FDB913", "#87CEEB"];
      for (const action of data) {
        if (action.team_id in teamColors === false) {
          if (Object.keys(teamColors).length >= 2)
            throw "You specified ids of teams that do not occur in the data!";
          else
            teamColors[action.team_id] = defaultColors[Object.keys(teamColors).length];
        }
      }
      var elTable = select_default(this);
      var tbody = tabulate(elTable, tableColumns);
      var uTable = tbody.selectAll("tr").data(data);
      var rows = uTable.enter().append("tr").attr("id", (d) => d.action_id);
      uTable.exit().remove();
      rows.selectAll("td").data(function(row, i) {
        return Object.keys(tableColumns).map(function(column) {
          return { column, value: row[column], i, data: row };
        });
      }).enter().append("td").each(function(d) {
        if (d.column === "symbol") {
          select_default(this).attr("class", d.column).style("text-align", "center").append("svg").attr("height", 20).attr("width", 20).call(
            actiontypePlotFn[d.data.type_id]["symbol"],
            d.data,
            d.i + 1,
            teamColors[d.data.team_id],
            scale
          ).select("g").attr("transform", "translate(10,10)");
        } else {
          select_default(this).attr("class", d.column).html(function(d2) {
            if (d2.column === "type_id") {
              return spadlActionTypes[+d2.value]["label"];
            } else if (d2.column === "result_id") {
              return spadlResults[+d2.value]["label"];
            } else if (d2.column === "bodypart_id") {
              return spadlBodyparts[+d2.value]["label"];
            } else {
              return d2.value;
            }
          });
        }
      });
    });
  }
  chart.tableColumns = function(d) {
    if (!arguments.length) return tableColumns;
    tableColumns = d;
    return chart;
  };
  chart.scale = function(_) {
    if (!arguments.length) return scale;
    scale = _;
    return chart;
  };
  chart.teamColors = function(_) {
    if (!arguments.length) return teamColors;
    teamColors = _;
    return chart;
  };
  return chart;
}

// node_modules/d3-soccer/src/charts/action_tooltip.js
function action_tooltip_default() {
  var tooltip = select_default("#tooltip");
  function chart(g) {
    if (!tooltip.size()) {
      tooltip = g.append("div").style("position", "absolute").style("pointer-events", "none").style("z-index", 6).style("opacity", 0).attr("class", "tooltip").attr("id", "tooltip");
    }
  }
  chart.show = function(e, d) {
    tooltip.html(`
        <table>
          <tr>
            <th colspan="2">${spadlActionTypes[d.type_id].label}</th>
          <tr>
            <td>Player</td>
            <td>${d.player_name || d.player_id}</td>
          </tr>
          <tr>
            <td>Team</td>
            <td>${d.team_name || d.team_id}</td>
            </tr>
          <tr>
            <td>Result</td>
            <td>${spadlResults[d.result_id].label}</td>
            </tr>
          <tr>
            <td>Body part</td>
            <td>${spadlBodyparts[d.bodypart_id].label}</td>
            </tr>
          <tr>
            <td>Time</td>
            <td>${Math.floor(d.time_seconds / 60)} min ${d.time_seconds % 60} sec</td>
            </tr>
          <tr>
            <td>Start</td>
            <td>x=${d.start_x.toFixed(2)} , y=${d.start_y.toFixed(2)}</td>
            </tr>
          <tr>
            <td>End</td>
            <td>x=${d.end_x.toFixed(2)} , y=${d.end_y.toFixed(2)}</td>
          </tr>
        </table>
      `);
    let [x, y] = pointer_default(e, document.body);
    tooltip.style("opacity", 0.95).style("left", x + "px").style("top", y + "px");
    return chart;
  };
  chart.hide = function() {
    tooltip.style("opacity", 0);
    return chart;
  };
  return chart;
}

// node_modules/d3-soccer/src/charts/scoreline.js
function scoreline_default() {
  var height = 23;
  var team_width = 45;
  var score_width = 40;
  var clock_width = 40;
  var width = 2 * team_width + score_width + clock_width;
  var teams = [
    { label: "H", color: "#87CEEB" },
    { label: "A", color: "#FDB913" }
  ];
  var score = [0, 0];
  var updateScore;
  var clock = 0;
  var updateClock;
  function chart(g) {
    g.each(function() {
      var elem = select_default(this);
      var elem_team1 = elem.append("g");
      elem_team1.append("rect").attr("x", 0).attr("y", 0).attr("width", team_width).attr("height", height).attr("fill", teams[0].color);
      elem_team1.append("text").text(teams[0].label).attr("x", 5).attr("y", height / 2).attr("dominant-baseline", "middle").attr("fill", "white");
      var elem_team2 = elem.append("g").attr("transform", `translate(${team_width + score_width}, 0)`);
      elem_team2.append("rect").attr("x", 0).attr("y", 0).attr("width", team_width + 6).attr("height", height).attr("fill", teams[1].color);
      elem_team2.append("text").text(teams[1].label).attr("x", 5).attr("y", height / 2).attr("dominant-baseline", "middle").attr("fill", "white");
      elem.append("text").attr("id", "scoreline").text(`${score[0]} : ${score[1]}`).attr("x", 5 + team_width).attr("y", height / 2).attr("dominant-baseline", "middle").attr("fill", "black");
      var elem_clock = elem.append("g");
      elem_clock.append("polyline").attr(
        "points",
        `${width - clock_width},${height / 2}
                         ${width - clock_width + 6},0
                         ${width},0
                         ${width},${height}
                         ${width - clock_width + 6},${height}`
      ).attr("fill", "white");
      elem_clock.append("text").attr("id", "clock").text(`${clock}'`).attr("x", width - clock_width + 15).attr("y", height / 2).attr("dominant-baseline", "middle").attr("fill", "black");
      elem.append("rect").attr("x", 0).attr("y", 0).attr("width", width).attr("height", height).attr("fill", "rgba(0,0,0,0)").attr("stroke", "black").attr("stroke-width", 0.5);
      updateClock = function() {
        elem.select("#clock").text(`${clock}'`);
      };
      updateScore = function() {
        elem.select("#scoreline").text(`${score[0]} : ${score[1]}`);
      };
    });
  }
  chart.height = function(_) {
    if (!arguments.length) return height;
    height = +_;
    return chart;
  };
  chart.teams = function(value) {
    if (!arguments.length) return teams;
    teams = value;
    return chart;
  };
  chart.score = function(value) {
    if (!arguments.length) return score;
    score = value;
    if (typeof updateScore === "function") updateScore();
    return chart;
  };
  chart.clock = function(value) {
    if (!arguments.length) return clock;
    clock = value;
    if (typeof updateClock === "function") updateClock();
    return chart;
  };
  return chart;
}

// node_modules/d3-soccer/src/charts/header.js
function header_default() {
  var hed = void 0;
  var subhed = void 0;
  var img = void 0;
  function chart(g) {
    g.each(function() {
      var header = select_default(this).append("g").attr("class", "header");
      var defs = header.append("defs").attr("id", "imgdefs");
      var imgpattern = defs.append("pattern").attr("id", "imgpattern").attr("height", 1).attr("width", 1).attr("x", "0").attr("y", "0");
      imgpattern.append("image").attr("x", 0).attr("y", 10).attr("height", 80).attr("xlink:href", img);
      header.append("circle").attr("class", "circle").attr("r", 40).attr("cy", 50).attr("cx", 50).attr("stroke", "#333").attr("fill", "url(#imgpattern)");
      header.append("text").attr("class", "h2").attr("x", 110).attr("y", 40).attr("font-size", "21px").attr("fill", "#333").text(subhed);
      header.append("text").attr("class", "h1").attr("x", 110).attr("y", 80).attr("font-weight", "bold").attr("font-size", "32px").attr("fill", "black").text(hed);
    });
  }
  chart.hed = function(_) {
    if (!arguments.length) return hed;
    hed = _;
    return chart;
  };
  chart.subhed = function(_) {
    if (!arguments.length) return subhed;
    subhed = _;
    return chart;
  };
  chart.img = function(_) {
    if (!arguments.length) return img;
    img = _;
    return chart;
  };
  return chart;
}

// node_modules/d3-soccer/src/charts/header_match.js
function header_match_default() {
  var hed = "Premier League";
  var logo_home = void 0;
  var logo_away = void 0;
  var score = [0, 0];
  function chart(g) {
    g.each(function() {
      var scoreboard = select_default(this).append("g").attr("id", "scoreboard").attr("class", ".hed");
      scoreboard.append("svg:image").attr("xlink:href", logo_home).attr("width", 40).attr("x", 0).attr("y", 25);
      scoreboard.append("svg:image").attr("xlink:href", logo_away).attr("width", 40).attr("x", 130).attr("y", 25);
      scoreboard.append("text").attr("x", 0).attr("y", 5).attr("dominant-baseline", "hanging").attr("font-size", "16px").attr("fill", "#333").text(hed);
      scoreboard.append("text").attr("x", 55).attr("y", 55).attr("font-weight", "bold").attr("font-size", "32px").attr("fill", "black").text(`${score[0]} : ${score[1]}`);
    });
  }
  chart.hed = function(_) {
    if (!arguments.length) return hed;
    hed = _;
    return chart;
  };
  chart.logoHome = function(_) {
    if (!arguments.length) return logo_home;
    logo_home = _;
    return chart;
  };
  chart.logoAway = function(_) {
    if (!arguments.length) return logo_away;
    logo_away = _;
    return chart;
  };
  chart.score = function(_) {
    if (!arguments.length) return score;
    score = _;
    return chart;
  };
  return chart;
}
export {
  action_tooltip_default as actionTooltip,
  actions_default as actions,
  actions_table_default as actionsTable,
  grid,
  header_default as header,
  heatmap_default as heatmap,
  header_match_default as matchHeader,
  pitch_default as pitch,
  rectbin,
  scoreline_default as scoreline
};
//# sourceMappingURL=d3-soccer.js.map
